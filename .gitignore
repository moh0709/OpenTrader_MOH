.idea
node_modules
.DS_Store
playground
.env
.env.production
config.json5
exchanges.json5
.vercel

# Release build for publishing on NPM
/release

# Prisma SQLite database files
dev.db
dev.db-journal

# pnpm prune
/out

/k8s

# TS
tsconfig.tsbuildinfo

# For some reason after `moon run :build`, the `/dts` is created in the root directory.
# We don't need to emit types at all, because we use a separate command for this: `moon run :dts`
# Workaround to avoid committing them to the repo.
/dts

# moon
.moon/cache
.moon/docker

# Local isolated development worktrees
.worktrees/

# Build backups created during manual deploys. deploy_adaptive_trailing.sh
# writes pre-/incoming-/outgoing- copies next to the live bundle on every run.
app/dist.bak-*
app/dist.incoming-*
app/dist.outgoing-*
app/dist.prebuild*
app/dist.pre-adaptive-trailing-*
*.bak-*
# Stray empty DB created by running the CLI from the repo root
/opentrader.db
